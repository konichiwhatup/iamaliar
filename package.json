{
  "name": "iamaliar-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "pages:build": "cd iamaliar-web-v3 && npm install && npm run pages:build && rm -rf ../dist && cp -r .vercel/output/static ../dist"
  }
}
